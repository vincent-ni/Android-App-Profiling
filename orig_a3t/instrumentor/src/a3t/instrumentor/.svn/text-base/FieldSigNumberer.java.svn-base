package a3t.instrumentor;

import soot.SootField;

public class FieldSigNumberer extends SigNumberer {
	public int getOrMakeId(SootField fld) {
		String s = fld.getSignature();
		return getNumber(s);
	} 
}
